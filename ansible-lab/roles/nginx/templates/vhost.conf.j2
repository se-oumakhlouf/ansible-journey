server {

    listen {{ nginx_port }};
    listen [::]:{{ nginx_port }};

    root {{ nginx_root }};
    index index.html;

    server_name {{ nginx_server_name }};

    location / {
        try_files $uri $uri/ =404;
    }
}